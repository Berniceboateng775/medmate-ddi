import 'package:flutter/material.dart';

class DoctorNotifications extends StatelessWidget {
  const DoctorNotifications({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Notifications (none yet)'));
}
