import 'package:flutter/material.dart';

class AdminRegisterUser extends StatelessWidget {
  const AdminRegisterUser({super.key});
  @override
  Widget build(BuildContext context) =>
      const Center(child: Text('Register User'));
}
